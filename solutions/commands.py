def solution(numbers, target):
    answer = 0
    length = len(numbers)
    for pick in range(length):
        for i in pick:
            pass

        return answer


def fact(input_num):
    if input_num == 1:
        return 1
    return input_num * fact(input_num - 1)

